{
  "name": "portfolio",
  "private": true,
  "description": "My personal portfilio",
  "version": "0.1.0",
  "author": "Mark Goetz",
  "dependencies": {
    "sass": "^1.54.8"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "npm-run-all": "^4.1.5"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./_site",
    "start:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start:sass": "sass scss/_main.scss _site/index.css --watch",
    "build:sass": "sass scss/_main.scss _site/index.css --style=compressed",
    "start": "yarn clean && run-p start:eleventy start:sass",
    "build": "yarn clean && yarn build:sass && yarn build:eleventy"
  }
}
